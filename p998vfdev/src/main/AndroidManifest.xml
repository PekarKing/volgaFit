<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.avgroup.p998vfdev">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/volgafit_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/volgafit_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AboutScroll" />
        <activity android:name=".MyTrains" />
        <activity android:name=".NotfActivity" />
        <activity android:name=".FilterActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".CurrentItem" />
    </application>

</manifest>